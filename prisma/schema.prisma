datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  email String @id
  userId String
  password String
  createdAt DateTime @default(now())
}

model Post {
  postId String @id
  createdAt DateTime @default(now())
}