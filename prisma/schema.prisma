datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id String @id
  email String
  userId String
  password String
  createdAt DateTime @default(now())
}

model Post {
  postId String @id
  id String
  contents String
  createdAt DateTime @default(now())
}